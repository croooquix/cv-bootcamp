{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: 현재 파일 디버깅 (PYTHONPATH 자동 연동)",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}/week2",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/week2"
            }
        }
    ]
}